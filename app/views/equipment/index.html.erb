<div class="top-space"></div>
<div class="card-container">
<% @equipment.each do |eq| %>

  <div class="card">
      <div class="equipment">
        <div class="image">
            <%= link_to cl_image_tag(eq.photos.first.key), equipment_path(eq)  %>
        </div>
      <div class="info">
        <h5 class="title"><%= eq.title %></h5>
        
        <p class="card_description"><%= eq.description %></p>
        <p class="card_price">£<%= eq.price %></p>


      <%# <div class="book_now"> %>
        <%# <button class="card_button">Show me</button> %>
            <%# <button class="button"><%= link_to 'Show me', equipment_path(eq) %>
    <%# </div> %>
      </div>
    </div>
  </div>
<% end %>
</div>

